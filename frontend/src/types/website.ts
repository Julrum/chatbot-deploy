export interface WebsiteProps {
  createdAt: string;
  description: string;
  disclaimer: string;
  id: string;
  imageUrl?: string;
  name: string;
  primaryColor?: string;
  secondaryColor?: string;
  url: string;
}
