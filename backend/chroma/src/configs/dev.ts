import {Express} from "express";
export const config = {
  chromaHost: "TODO: set your chroma host here",
  // eslint-disable-next-line @typescript-eslint/no-empty-function
  listen: (app: Express) => {},
};
